{
  "name": "makemake",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "start": "node dist/index.js",
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node dist/tests/recommendation.test.js && node dist/tests/server.smoke.test.js",
    "dev": "npm run build && node dist/index.js",
    "dev:server": "npm run build && node dist/server/server.js"
  }
}
